#include <iostream>
using namespace std;

void imprimirTriangulo(int altura) {
    // Implementar funcion aqui
}

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    imprimirTriangulo(a);
    imprimirTriangulo(b);
    imprimirTriangulo(c);
    return 0;
}
